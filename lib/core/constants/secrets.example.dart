// ── secrets.example.dart ────────────────────────────────────────────────────
// Copy this file to secrets.dart and replace the placeholder values.
// secrets.dart is listed in .gitignore and will never be committed.

abstract class Secrets {
  static const cloudName = 'YOUR_CLOUDINARY_CLOUD_NAME';
  static const cloudinaryApiKey = 'YOUR_CLOUDINARY_API_KEY';
  static const cloudinaryApiSecret = 'YOUR_CLOUDINARY_API_SECRET';
}
